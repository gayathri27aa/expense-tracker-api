ENVIRONMENT=development
DATABASE_URL=postgresql+asyncpg://<user>:<password>@localhost:5432/expense_tracker

# JWT — generate a strong key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-generate-a-real-key-before-deploying
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
